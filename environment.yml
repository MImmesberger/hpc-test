name: hpc-test
channels:
  - conda-forge
dependencies:
  - python=3.14
  - numpy
  - pandas
  - pip
  - pip:
    - pytask>=0.5.2
    - pytask-parallel
    - cloudpickle
    - attrs>=21.3
    - -e ../pytask-slurm
    - -e .
